name: planet_logistics_app
description: Professional logistics tracking app for Android.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_maps_flutter: ^2.6.1
  socket_io_client: ^2.0.3+1
  geolocator: ^11.0.0
  permission_handler: ^11.3.1
  http: ^1.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/
