name: planet_logistics
version: 0.1.0+1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.7.0
  socket_io_client: ^2.0.3+1
  geolocator: ^12.0.0
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
